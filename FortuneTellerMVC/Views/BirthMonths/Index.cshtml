@model IEnumerable<FortuneTellerMVC.Models.BirthMonth>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BirthMonthName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BirthMonthNum)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BirthMonthName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BirthMonthNum)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BirthMonthID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BirthMonthID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BirthMonthID })
        </td>
    </tr>
}

</table>
